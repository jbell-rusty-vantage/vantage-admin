"use client";

/**
 * UI2-USERS: the Users tab's entry for the Registry shell. The Sales Intelligence stylesheet (tokens and the
 * `/* UI-2: UI2-USERS *\/` block) is imported here, never from a module the render tests load.
 */
import "@/components/sales-intelligence/styles/sales-intelligence.css";

export { UsersPanel } from "./users-tab";
