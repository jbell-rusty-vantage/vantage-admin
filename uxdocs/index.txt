Write text wireframes and ui / ux suggestions in this directory !

These files are suggestions unless a doc says the surface is live.

Live: dashboard chrome
  — Sidebar grouped Today / Records / People / Insight / System in
    components/layout/dashboard-nav.tsx. visibleDashboardNav(role) is
    still a flattened destination list (Owner starts Overview, Live
    Events, Lead Conversations). Duplicate Form/Call Leads nest under
    their parents in the UI. Job Timeline is title case. Compact 13px
    medium rows; nested duplicates sit on a left rail. No New badges.
    Navbar: command palette (⌘K / Ctrl+K via GlobalSearch +
    command-palette.ts), compact database scope selector, UserMenu,
    pageTitleForPath. Aside sticky + h-screen (w-56 expanded, w-14
    collapsed); sidebar and mobile drawer scroll. Not Owner Daily View.
    Authority: .cursor/rules/project-organization.mdc (components/layout).

Live: Overview desk at /
  — HomeOverview: Owner Waiting for you Booking intakes (existing
    open-case list, limit 3), this-week pulse, compact all-time, create
    Booking/Cancellation. Cancellation intakes are not on Overview.
    Not Daily View, not a sitemap, not a second Analytics. Authority:
    .cursor/rules/project-organization.mdc (components/dashboard).

Live: Form Lead contact snapshots
  — Admin /form-leads and /duplicate-form-leads show Form submitted vs
    Granot contact (chip + Contacts detail). Search hits live + ingested +
    Granot snapshot. Call Leads unchanged.
    Authority: vantage-main-server/docs/form-lead-contact-snapshots-display-and-search-specification.md

Contract (not fully developed): Booking intake Form Lead contact snapshots
  — Find the right customer on /intakes should search and show Form
    submitted vs Granot. Owner will add more to this file. Do not implement.
    Authority: vantage-main-server/docs/granot-lead-lifecycle/booking-intake-form-lead-contact-snapshots-specification.md

Live: live-events-tab-specification.md
  — Owner-only Granot live webhook SSE as a top-level sidebar tab
    "Live Events" (immediately under Overview) at /live-events.
    Contract: live-events-tab-specification.md. Do not rebuild the stream.

Live: Lead Conversations tab at /conversations
  — Owner-only example card for the seeded inbound Call Lead conversation.
    Contract: lead-conversations-tab-specification.md (wins on conflict).
    HANDOFF-conversations-example-tab.md is post-ship notes. Do not rebuild
    the tab, add a second seed, or treat AI Gateway as live.

Start here for Job Timeline Enhancement (Admin): HANDOFF-job-timeline-enhancement.md
  — orientation for JTE-04 / JTE-05. Do not start until JTE-03 is complete
    on the server. Contract: vantage-main-server/docs/job-number-timeline/.

Live: operational surfaces (Admin) — HANDOFF-operational-surfaces.md
  — OSE-01–05 shipped. Keep OperationalResourcePage. Tabbed
    detail panel, clustered row actions, grouped filters, no JSON.
    Contract: vantage-main-server/docs/operational-surfaces/.
    21st.dev allowed for the four named shells only. Not Daily View.

Start here for Daily View work: HANDOFF-owner-daily-view.md
  — orientation for an agent picking up the Owner Daily View in this repo.
    Points at the spec, the nine unit contracts, and the wireframes.

Architected, not implemented: owner-daily-view-planned.txt
  — text wireframes for the Owner Daily View. The contract lives in
    vantage-main-server/docs/granot-lead-lifecycle/owner-daily-operations-view-specification.md
    Where the wireframes and the specification disagree, the specification wins.
    The RingCentral conversation pipeline inside it is DEFERRED (spec 5.0).

